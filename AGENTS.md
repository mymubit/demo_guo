# AGENTS.md

## 项目概述

- 项目为 ScriptForge，包含 Django 后端、React 前端、运营后台与大模型相关配置能力。
- 后端主目录：`backend/`，核心代码按 Django app 拆分在 `backend/apps/`。
- 前端主目录：`frontend/`，页面、组件、服务封装在 `frontend/src/`。
- 修改代码前先阅读相关调用链，保持最小修改范围，优先复用已有服务、组件和工具函数。

### Drama Skills 系统（新核心功能）

- 短剧创作技能库：`drama-skills/`（根目录）— 36个专业角色，8个职能部门
- Drama Web App：`backend/apps/drama/` + `frontend/src/pages/Drama/`
- 技能安装说明：`drama-skills/README.md`
- 网站技术方案：`docs/DRAMA-SKILLS-WEBSITE-PLAN.md`
- 路由：`/drama`（创作中心）、`/drama/workspace/:id`（工作台）、`/drama/scripts/:id`（剧本展示）
- Admin：`/admin/drama-models`（模型配置 + Token统计）

## 全局原则

- 默认使用中文沟通；代码标识、库名、协议名保留英文。
- 优先级：正确性 > 数据安全 > 向后兼容 > 可维护性 > 性能 > 优雅设计。
- 不随意引入第三方依赖；确需新增时必须说明用途，并优先对齐现有技术栈。
- 不重构无关代码，不格式化无关文件，不回滚他人未提交修改。
- 禁止硬编码密钥、Token、API Key、生产地址和用户敏感信息。

## AI 行为准则

- 编码前先说明关键假设、目标完成标准和可能的方案取舍；拿不准时先提问。
- 简洁优先，用最少代码解决实际问题；不提前开发未要求的预判性功能。
- 精准改动，只修改完成任务必需的代码；没有故障的代码不要顺手重构。
- 先阅读再编写，新建或改动文件前必须理解周边已有实现和项目风格。
- 遵循项目规范优于自创新奇写法；存在风格冲突时选择项目中更主流的一种。
- 逻辑判断优先交由确定性代码实现，不把可编码的业务决策交给模型文本推理。
- 错误必须显性反馈，禁止静默失败；跳过、不完整或断言失败都要明确说明。
- 长耗时任务按步骤校验中间状态，避免某一步失败导致此前进度不可追踪。

## 后端规范

- 遵循 Django MTV 与 DRF 分层：View 负责请求响应，Serializer 负责序列化和参数校验，核心业务逻辑放到 service / domain 层。
- 数据库访问必须使用 Django ORM，禁止拼接 SQL；确需原生 SQL 时必须使用参数化查询。
- 多表读取注意 N+1 问题，按需使用 `select_related` / `prefetch_related`。
- 多表写入、关键状态变更和扣费类操作必须使用 `transaction.atomic()`。
- 批量写入优先使用 `bulk_create` / `bulk_update`，避免循环 `save()`。
- API 入参必须校验，权限控制通过 DRF permission 或既有权限体系实现。
- Model 建议显式指定 `db_table`，字段命名保持业务语义清晰。

## 配置与安全

- 敏感配置只允许通过环境变量、加密配置表或项目既有配置服务读取。
- 不在日志、接口响应、异常信息中输出密钥、手机号、Token、原始请求头等敏感信息。
- 生产环境必须关闭 DEBUG，避免暴露调试页面和内部错误堆栈。
- 文件上传、外部 URL、第三方回调、LLM 输出等外部输入默认不可信，使用前必须校验。
- 删除、覆盖、批量更新等高风险操作必须确认影响范围，并优先保留可审计记录。

## 日志与异常

- 使用项目 logger，禁止用 `print` / `console.log` 替代正式日志。
- 异常捕获必须指定具体异常类型；确需兜底捕获时必须记录上下文并给出可追踪信息。
- 禁止捕获异常后静默忽略。
- 返回给前端的错误信息应用户可读，不直接暴露数据库错误、堆栈或内部实现。
- 关键业务操作日志应包含操作对象、结果和必要上下文，但不得包含敏感数据。

## 前端规范

- 使用 React 函数组件和 Hooks，遵守 Hooks 调用规则。
- 组件、页面文件命名使用 PascalCase；工具函数、hooks、service 按现有目录规范放置。
- 组件 props 需要有清晰结构；新增 TypeScript 代码时禁止使用 `any`。
- 事件处理函数以 `handle` 开头，对外事件 props 以 `on` 开头。
- 接口请求统一走 `frontend/src/services/` 下的封装，避免在组件中散落请求细节。
- 样式保持项目既有 Tailwind / 组件风格，不混用新的样式方案。
- 列表渲染必须使用稳定唯一 key，禁止使用数组索引作为业务列表 key。

## 大模型与外部服务

- 大模型 Provider、模型名、Base URL、API Key 等配置必须走项目既有配置体系。
- LLM 输出写入业务数据前必须做结构校验和容错处理，不能直接信任模型返回。
- Prompt、规则、模板类配置优先使用项目已有后台配置或集中配置文件，避免散落硬编码。
- 外部服务调用必须处理超时、失败、重试边界和错误日志。

## 测试规范

- 修改核心服务、权限、计费、配置加载、数据合并、外部调用等逻辑时必须补充或更新测试。
- 测试至少覆盖正常路径、边界值、异常输入和权限/配置缺失场景。
- 单元测试应可重复执行，不依赖真实外部网络服务。
- 修复 bug 时优先补充能复现问题的测试，再实现修复。
- 测试必须验证真实业务约束，禁止编写因错误原因碰巧通过的弱测试。

## 提交与协作

- 提交前检查 `git diff`，确保只包含本次任务相关修改。
- 不提交 `.env`、密钥文件、临时日志、构建产物和本地缓存。
- 接口字段、响应结构、数据库结构发生变化时，必须同步更新前后端调用和测试。
- 变更公共契约时在说明中明确影响范围、兼容策略和验证方式。

## 禁止事项

- 禁止绕过项目既有架构直接把业务逻辑写进 View 或组件。
- 禁止新增重复的请求封装、配置读取、加密、日志工具。
- 禁止在循环中执行可批量完成的数据库查询或写入。
- 禁止忽略 linter / 测试中与本次修改相关的错误。
- 禁止为了通过测试而删除有效断言或放宽业务约束。

interface:
  display_name: "Create Project Brief"
  short_description: "Turn a creative request into a traceable project brief"
  default_prompt: "Use $create-project-brief to create a project brief from my requirements."
policy:
  allow_implicit_invocation: true

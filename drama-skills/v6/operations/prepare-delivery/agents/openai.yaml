interface:
  display_name: "Prepare Delivery"
  short_description: "Prepare a gated production and delivery package"
  default_prompt: "Use $prepare-delivery to evaluate and package the approved script for delivery."
policy:
  allow_implicit_invocation: true

name: Validate Drama Skills

on:
  pull_request:
  push:
    branches: [drama-skills]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -r requirements-dev.txt
      - run: python build/validate_skills.py
      - run: python build/validate_theme_matrix.py
      - run: python build/validate_workbench.py
      - run: python build/validate_artifacts.py
      - run: python build/validate_config.py
      - run: python build/validate_workflow.py
      - run: python build/validate_quality_cases.py
      - run: python -m unittest discover -s build/tests -p "test_*.py" -v

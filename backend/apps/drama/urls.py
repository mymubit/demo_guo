"""Drama API 路由。"""
from django.urls import path

from apps.drama.views import (
    AdminConfigRollbackView,
    AdminConfigView,
    AdminLlmCallLogListView,
    AdminLlmProviderActivateView,
    AdminLlmProviderDetailView,
    AdminLlmProviderListCreateView,
    AdminLlmProviderTestView,
    AdminSkillOpsExportView,
    AdminSkillOpsOverviewView,
    ArtifactDetailView,
    ExternalScriptReviewView,
    LlmCallLogDetailView,
    ProjectLlmCallLogListView,
    GenerationJobDetailView,
    GenerationJobLlmCallLogListView,
    GenerationJobReprocessView,
    GenerationJobSSEView,
    GenerationSSEView,
    GenerationAbandonView,
    GenerationLatestView,
    GenerationStartView,
    GenerationStatusView,
    ProjectDetailView,
    ProjectListCreateView,
    ProjectSettingsView,
    RoleBundleContentView,
    RolePromptBreakdownView,
    SkillsContentView,
    SkillsInventoryView,
    StoryBibleApprovalView,
    ThemeMatrixView,
    WorkbenchFormView,
    WorkflowCommandView,
    WorkflowStateView,
)

urlpatterns = [
    path("projects/", ProjectListCreateView.as_view(), name="drama-projects"),
    path("projects/<uuid:project_id>/", ProjectDetailView.as_view(), name="drama-project-detail"),
    path(
        "projects/<uuid:project_id>/settings/",
        ProjectSettingsView.as_view(),
        name="drama-project-settings",
    ),
    path(
        "projects/<uuid:project_id>/workflow/",
        WorkflowStateView.as_view(),
        name="drama-workflow",
    ),
    path(
        "projects/<uuid:project_id>/workflow/commands/",
        WorkflowCommandView.as_view(),
        name="drama-workflow-commands",
    ),
    path(
        "projects/<uuid:project_id>/approvals/story-bible/",
        StoryBibleApprovalView.as_view(),
        name="drama-story-bible-approval",
    ),
    path(
        "projects/<uuid:project_id>/artifacts/<str:artifact_key>/",
        ArtifactDetailView.as_view(),
        name="drama-artifact",
    ),
    path(
        "projects/<uuid:project_id>/generation/start/",
        GenerationStartView.as_view(),
        name="drama-generation-start",
    ),
    path(
        "projects/<uuid:project_id>/generation/latest/",
        GenerationLatestView.as_view(),
        name="drama-generation-latest",
    ),
    path(
        "projects/<uuid:project_id>/generation/<uuid:job_id>/",
        GenerationStatusView.as_view(),
        name="drama-generation-status",
    ),
    path(
        "projects/<uuid:project_id>/generation/<uuid:job_id>/abandon/",
        GenerationAbandonView.as_view(),
        name="drama-generation-abandon",
    ),
    path(
        "projects/<uuid:project_id>/generation/<uuid:job_id>/stream/",
        GenerationSSEView.as_view(),
        name="drama-generation-sse",
    ),
    path(
        "projects/<uuid:project_id>/llm-logs/",
        ProjectLlmCallLogListView.as_view(),
        name="drama-project-llm-logs",
    ),
    path("llm-logs/<uuid:log_id>/", LlmCallLogDetailView.as_view(), name="drama-llm-log-detail"),
    path("jobs/<uuid:job_id>/", GenerationJobDetailView.as_view(), name="drama-job-detail"),
    path(
        "jobs/<uuid:job_id>/llm-logs/",
        GenerationJobLlmCallLogListView.as_view(),
        name="drama-job-llm-logs",
    ),
    path(
        "jobs/<uuid:job_id>/reprocess/",
        GenerationJobReprocessView.as_view(),
        name="drama-job-reprocess",
    ),
    path(
        "jobs/<uuid:job_id>/stream/",
        GenerationJobSSEView.as_view(),
        name="drama-job-sse",
    ),
    path(
        "external-script-reviews/",
        ExternalScriptReviewView.as_view(),
        name="drama-external-review",
    ),
    path("theme-matrix/", ThemeMatrixView.as_view(), name="drama-theme-matrix"),
    path("meta/workbench-form/", WorkbenchFormView.as_view(), name="drama-workbench-form"),
    path(
        "meta/skills-inventory/",
        SkillsInventoryView.as_view(),
        name="drama-skills-inventory",
    ),
    path(
        "meta/roles/<str:agent_id>/prompt-breakdown/",
        RolePromptBreakdownView.as_view(),
        name="drama-role-prompt-breakdown",
    ),
    path(
        "meta/roles/<str:agent_id>/bundle-content/",
        RoleBundleContentView.as_view(),
        name="drama-role-bundle-content",
    ),
    path(
        "meta/skills-content/",
        SkillsContentView.as_view(),
        name="drama-skills-content",
    ),
    path("admin/config/", AdminConfigView.as_view(), name="drama-admin-config"),
    path(
        "admin/config/rollback/",
        AdminConfigRollbackView.as_view(),
        name="drama-admin-config-rollback",
    ),
    path(
        "admin/llm/providers/",
        AdminLlmProviderListCreateView.as_view(),
        name="drama-admin-llm-providers",
    ),
    path(
        "admin/llm/providers/<uuid:provider_id>/",
        AdminLlmProviderDetailView.as_view(),
        name="drama-admin-llm-provider-detail",
    ),
    path(
        "admin/llm/providers/<uuid:provider_id>/activate/",
        AdminLlmProviderActivateView.as_view(),
        name="drama-admin-llm-provider-activate",
    ),
    path(
        "admin/llm/providers/<uuid:provider_id>/test/",
        AdminLlmProviderTestView.as_view(),
        name="drama-admin-llm-provider-test",
    ),
    path(
        "admin/llm/logs/",
        AdminLlmCallLogListView.as_view(),
        name="drama-admin-llm-logs",
    ),
    path(
        "admin/llm/test/",
        AdminLlmProviderTestView.as_view(),
        name="drama-admin-llm-test",
    ),
    path(
        "admin/skill-ops/",
        AdminSkillOpsOverviewView.as_view(),
        name="drama-admin-skill-ops",
    ),
    path(
        "admin/skill-ops/export/",
        AdminSkillOpsExportView.as_view(),
        name="drama-admin-skill-ops-export",
    ),
]

---
description: ScriptForge 全局工程协作与代码修改规范
alwaysApply: true
---

# ScriptForge 全局工程规范

- 默认使用中文沟通；代码、配置、依赖名保持项目原有语言风格，新增注释优先中文且只解释必要业务意图。
- 决策优先级：正确性 > 数据安全 > 向后兼容 > 可维护性 > 性能 > 优雅设计。
- 修改前先理解调用链和现有约定，保持最小改动范围；不要顺手重构无关模块。
- 禁止随意新增第三方依赖；确需新增时，必须说明用途、替代方案和引入风险。
- 业务魔法值必须抽为常量或枚举；禁止硬编码状态、支付方式、权限字段、错误码。
- 函数保持单一职责，优先卫语句降低嵌套；复杂逻辑应下沉到合适的 service、hook 或 util。
- 不要绕过项目既有架构：后端遵循 Django MTV + DRF，前端通过 services 层访问接口。
- 涉及接口契约、权限、金额、订单、支付、生产配置、数据删除时，必须提高安全级别并说明风险。
- 修改核心逻辑后，同步更新相关测试、契约文档或说明；不能验证时要明确列出未验证项。

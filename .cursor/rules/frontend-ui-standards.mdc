---
description: ScriptForge 前端 UI 规范
globs: frontend/src/**/*.{js,jsx,css},frontend/tailwind.config.js
alwaysApply: false
---

# ScriptForge 前端 UI 规范

- 仅做视觉改版时，不修改接口请求、业务状态、变量命名、数据结构和权限逻辑。
- 样式优先复用 `frontend/src/components/ui` 的 `Button`、`Card`、`Input`、`Textarea`、`Badge`、`Pagination`、`MetricCard`、`EmptyState`。
- 条件 class 必须使用 `cn()`，避免复杂模板字符串；重复 6 个以上 Tailwind class 的结构应抽为组件。
- 色彩默认使用 `navy`、`gold` 和语义色 `success/warning/danger/info`，不要随意混用 `gray/slate/purple/amber/red/green`。
- 图标尺寸优先使用 `ICON` 常量；图标和文字默认 `inline-flex items-center gap-2`。
- 动画优先使用 `frontend/src/constants/motion.js` 的模板，避免大幅弹跳、旋转和多层 stagger。
- 图表样式统一走 `frontend/src/components/charts/theme.js` 与 `options.js`，页面不要散写 ECharts 颜色和 grid。

---
description: ScriptForge Django 和 DRF 后端开发规范
globs: backend/**/*.py
alwaysApply: false
---

# Django / DRF 后端规范

- 按业务领域维护 app，视图只负责请求编排；核心业务逻辑放入 `services.py`，序列化与参数校验放入 serializer。
- API 基于 DRF 实现，权限通过 DRF permission 类表达，禁止在视图里散写权限判断。
- 配置按环境拆分，密钥、Token、支付配置、LLM 配置必须从环境变量读取，禁止硬编码。
- Model 枚举使用 `models.TextChoices` / `IntegerChoices`；核心表建议显式声明 `db_table`。
- 数据库访问必须使用 Django ORM；除非明确要求，不手写 SQL。确需原生 SQL 时必须参数化。
- 多表读取注意 N+1，按关系使用 `select_related` / `prefetch_related`；高频查询索引必须有真实查询依据。
- 多表写、金额、订单、会员权益、库存/额度变更必须使用 `transaction.atomic`；并发更新优先依赖数据库约束。
- 批量操作优先 `bulk_create` / `bulk_update`，避免循环 `save()`。
- 异常捕获必须指定具体异常；禁止裸 `except` 和静默吞错，系统错误不得直接暴露给前端。
- 日志使用项目 logger，禁止 `print`；日志不得包含密码、Token、密钥、完整支付凭证等敏感数据。

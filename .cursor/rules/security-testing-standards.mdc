---
description: ScriptForge 安全、测试与发布前验证规范
alwaysApply: true
---

# 安全与测试规范

- 所有外部输入、接口参数、上传内容、回调数据默认不可信，必须校验类型、范围、权限和业务状态。
- 数据修改接口必须做权限校验；删除操作、批量操作、金额与权益变更必须先说明风险。
- 密码使用 Django 自带哈希机制；禁止明文存储或在日志、响应、异常信息中输出敏感数据。
- 生产环境必须关闭 `DEBUG`，并禁用调试工具；配置中的密钥、数据库、Redis、对象存储、支付参数均走环境变量。
- 业务错误给用户可读提示；系统错误进入统一异常处理，不向前端暴露堆栈、SQL 或内部路径。
- 普通逻辑默认覆盖正常、边界、异常、非法输入和权限场景；核心工具类、公共接口、金额/订单逻辑必须补测试。
- 单元测试应可重复执行，不依赖外部网络；需要数据库时使用测试数据库和可控 fixture。
- 前端交互修改至少验证加载态、空态、错误态、权限态和主流程；浏览器问题要结合控制台与网络请求定位。
- 代码修改后优先运行关联测试、lint 或类型检查；不能运行时，在回复中明确原因和剩余风险。
- 发布或合并前确认迁移可回滚、接口契约同步、日志不泄密、关键路径具备必要监控或审计记录。

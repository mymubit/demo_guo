# Review Package Task 3
BASE: 4972919b6e219558503da4174d873c9100024a3e
HEAD: 6173287741bfc29c647076ce6ebfbf970ebec923
## Commits

## Stat
 .../drama/tests/test_judge_prompt_injection.py     | 18 ++++++++++++++++  .../src/components/artifacts/ArtifactViews.tsx     |  4 ++--  frontend/src/components/ui/Badge.tsx               |  4 ++--  frontend/src/components/ui/Tabs.tsx                |  4 ++--  frontend/src/components/ui/badge-tabs.test.tsx     | 24 ++++++++++++++++++++++  .../components/workbench/GenerationJobPanel.tsx    |  2 +-  frontend/src/components/workbench/ModulePanel.tsx  |  2 +-  frontend/src/components/workbench/PipelineRail.tsx |  2 +-  frontend/src/pages/ProjectListPage.tsx             |  2 +-  9 files changed, 52 insertions(+), 10 deletions(-)
## Diff
diff --git a/backend/apps/drama/tests/test_judge_prompt_injection.py b/backend/apps/drama/tests/test_judge_prompt_injection.py index 88ed21e..8227ecd 100644 --- a/backend/apps/drama/tests/test_judge_prompt_injection.py +++ b/backend/apps/drama/tests/test_judge_prompt_injection.py @@ -67,20 +67,38 @@ class JudgeRuleInjectionTests(SimpleTestCase):              scoring_mode="external",          )          self.assertIn("## 璇勫垎缁嗗垯锛堝唴鑱旓級", system)          self.assertIn("鍙欎簨鏁堢巼", system)          self.assertIn("浠樿垂鐐逛紭鍖?, system)          self.assertIn("0.15", system)          self.assertIn("scoring_preset=standard", system)          self.assertIn("evidence", system)          self.assertIn("deductions", system)   +    def test_scorer_prompt_includes_quality_report_skeleton_keys(self) -> None: +        builder = PromptBuilder(loader=self.loader) +        system, _ = builder.build( +            "drama.script-scorer", +            settings=_SETTINGS, +            workflow_state={}, +            artifacts={}, +            latest_script={ +                "resolved_script_key": "external_script", +                "value": {"episodes": [{"script": "x"}]}, +            }, +            scoring_mode="external", +        ) +        self.assertIn("evidence", system) +        self.assertIn("deductions", system) +        self.assertIn("dimensions", system) +        self.assertRegex(system, r"format|hooks|genre_fit") +      def test_scorer_rules_fit_budget_without_truncating_core(self) -> None:          text = self.loader.collect_rules(              "drama.script-scorer", _SETTINGS, max_chars=3600          )          self.assertNotIn("...", text[-5:])  # 鏍稿績瑙勫垯搴斿畬鏁磋鍏ワ紱鑻ヤ粛鎴柇闇€鍏堟彁 max_chars 鍐嶆祴          self.assertIn("鍗佺淮鏉冮噸鎽樿", text)          self.assertIn("S绾т竴绁ㄥ惁鍐?, text)        def test_compliance_prompt_keeps_nine_dimension_and_phases(self) -> None:          text = self.loader.collect_rules( diff --git a/frontend/src/components/artifacts/ArtifactViews.tsx b/frontend/src/components/artifacts/ArtifactViews.tsx index 5a28efd..3e31ac2 100644 --- a/frontend/src/components/artifacts/ArtifactViews.tsx +++ b/frontend/src/components/artifacts/ArtifactViews.tsx @@ -13,21 +13,21 @@ export function ProjectBriefView({ data }: { data: Record<string, unknown> }) {    const genre = data.genre_matrix as Record<string, string> | undefined    return (      <div className="space-y-4">        <header>          <h3 className="text-xl font-semibold text-ink">{String(data.title ?? '椤圭洰绠€鎶?)}</h3>          <p className="mt-2 text-sm text-ink-muted">{String(data.core_idea ?? '')}</p>        </header>        <div className="flex flex-wrap gap-2">          {genre            ? Object.entries(genre).map(([k, v]) => ( -              <Badge key={k} tone="brand"> +              <Badge key={k} tone="action">                  {GENRE_AXIS_LABELS[k] ?? k}锛歿v}                </Badge>              ))            : null}          {data.compliance_risk ? <Badge tone="warning">椋庨櫓 {String(data.compliance_risk)}</Badge> : null}        </div>        <dl className="grid grid-cols-2 gap-4 text-sm">          {(            [              ['鐩爣鍙椾紬', data.target_audience], @@ -284,21 +284,21 @@ export function NarrativePlanView({ data }: { data: Record<string, unknown> }) {    return (      <div className="space-y-3">        <h3 className="text-lg font-semibold text-ink">鍒嗛泦鍙欎簨璁″垝</h3>        <div className="grid grid-cols-1 gap-3">          {cards.map((card) => (            <article key={String(card.episode)} className="sf-panel p-4">              <div className="flex items-center justify-between gap-3">                <h4 className="font-medium text-ink">                  EP{String(card.episode)} 路 {String(card.title)}                </h4> -              <Badge tone="brand">{String(card.hook_grade ?? '')}</Badge> +              <Badge tone="action">{String(card.hook_grade ?? '')}</Badge>              </div>              <p className="mt-2 text-sm text-ink-muted">{String(card.core_event ?? '')}</p>              <div className="mt-3 grid grid-cols-2 gap-2 text-xs text-ink-muted">                <div>寮€鍦洪挬瀛愶細{String(card.opening_hook ?? '鈥?)}</div>                <div>缁撳熬閽╁瓙锛歿String(card.ending_hook ?? '鈥?)}</div>                <div>鎯呯华寮哄害锛歿String(card.emotion_intensity ?? '鈥?)}</div>                <div>浠樿垂鍗＄偣锛歿String(card.paywall_hook ?? '鈥?)}</div>              </div>            </article>          ))} diff --git a/frontend/src/components/ui/Badge.tsx b/frontend/src/components/ui/Badge.tsx index b5d4357..fe1adc2 100644 --- a/frontend/src/components/ui/Badge.tsx +++ b/frontend/src/components/ui/Badge.tsx @@ -1,17 +1,17 @@  import type { ReactNode } from 'react'  import { cn } from '@/utils/cn'   -type Tone = 'brand' | 'gold' | 'success' | 'warning' | 'danger' | 'info' | 'default' +type Tone = 'action' | 'gold' | 'success' | 'warning' | 'danger' | 'info' | 'default'    const toneClass: Record<Tone, string> = { -  brand: 'bg-brand-50 text-brand-700 border-brand-100', +  action: 'bg-action/10 text-action border-action/20',    gold: 'bg-amber-50 text-amber-800 border-amber-100',    success: 'bg-emerald-50 text-emerald-700 border-emerald-100',    warning: 'bg-amber-50 text-amber-700 border-amber-100',    danger: 'bg-red-50 text-red-700 border-red-100',    info: 'bg-sky-50 text-sky-700 border-sky-100',    default: 'bg-slate-100 text-slate-700 border-slate-200',  }    export function Badge({    tone = 'default', diff --git a/frontend/src/components/ui/Tabs.tsx b/frontend/src/components/ui/Tabs.tsx index 66876f7..ddb5609 100644 --- a/frontend/src/components/ui/Tabs.tsx +++ b/frontend/src/components/ui/Tabs.tsx @@ -19,27 +19,27 @@ export function Tabs({          return (            <button              key={item.id}              type="button"              role="tab"              aria-selected={active}              disabled={item.disabled}              onClick={() => onChange(item.id)}              className={cn(                'relative px-3 py-2 text-sm font-medium transition', -              active ? 'text-brand-600' : 'text-ink-muted hover:text-ink', +              active ? 'text-action' : 'text-ink-muted hover:text-ink',                item.disabled && 'cursor-not-allowed opacity-40',              )}            >              {item.label}              {active ? ( -              <span className="absolute inset-x-2 -bottom-px h-0.5 rounded-full bg-brand-500" /> +              <span className="absolute inset-x-2 -bottom-px h-0.5 rounded-full bg-action" />              ) : null}            </button>          )        })}      </div>    )  }    export function EmptyState({    title, diff --git a/frontend/src/components/ui/badge-tabs.test.tsx b/frontend/src/components/ui/badge-tabs.test.tsx new file mode 100644 index 0000000..08fd1d6 --- /dev/null +++ b/frontend/src/components/ui/badge-tabs.test.tsx @@ -0,0 +1,24 @@ +import { render, screen } from '@testing-library/react' +import { describe, expect, it } from 'vitest' +import { Badge } from './Badge' +import { Tabs } from './Tabs' + +describe('Badge and Tabs accents', () => { +  it('action tone avoids brand classes', () => { +    render(<Badge tone="action">杩涜涓?/Badge>) +    expect(screen.getByText('杩涜涓?).className).not.toMatch(/brand/) +    expect(screen.getByText('杩涜涓?).className).toMatch(/action/) +  }) + +  it('tabs active uses action underline', () => { +    render( +      <Tabs +        items={[{ id: 'a', label: '鍏ㄩ儴' }]} +        value="a" +        onChange={() => undefined} +      />, +    ) +    const tab = screen.getByRole('tab', { name: '鍏ㄩ儴' }) +    expect(tab.className).toMatch(/text-action/) +  }) +}) diff --git a/frontend/src/components/workbench/GenerationJobPanel.tsx b/frontend/src/components/workbench/GenerationJobPanel.tsx index d74b19c..3ca4a42 100644 --- a/frontend/src/components/workbench/GenerationJobPanel.tsx +++ b/frontend/src/components/workbench/GenerationJobPanel.tsx @@ -101,21 +101,21 @@ export function GenerationJobPanel({    }, [job?.job_id, projectId])      if (!job) return null      const badgeTone =      status === 'completed'        ? 'success'        : status === 'failed'          ? 'danger'          : status === 'running' -          ? 'brand' +          ? 'action'            : status === 'disabled'              ? 'warning'              : 'default'      const clamped = Math.min(100, Math.max(0, progress))    const showTrouble = status === 'failed' || status === 'disabled'      return (      <section className="sf-panel p-4" aria-label="鐢熸垚浠诲姟杩涘害">        <div className="flex items-center justify-between gap-3"> diff --git a/frontend/src/components/workbench/ModulePanel.tsx b/frontend/src/components/workbench/ModulePanel.tsx index 614c400..0833153 100644 --- a/frontend/src/components/workbench/ModulePanel.tsx +++ b/frontend/src/components/workbench/ModulePanel.tsx @@ -87,21 +87,21 @@ export function ModulePanel({                  {DOMAIN_LABELS[domain] ?? domain}                </div>                <ul className="space-y-1.5">                  {items.map((m) => (                    <li                      key={m.id}                      className="rounded-lg border border-slate-200 bg-canvas px-3 py-2"                    >                      <div className="flex items-center justify-between gap-2">                        <span className="text-sm text-ink">{m.label_zh}</span> -                      <Badge tone={m.kind === 'core' ? 'brand' : 'default'}> +                      <Badge tone={m.kind === 'core' ? 'action' : 'default'}>                          {KIND_LABELS[m.kind] ?? m.kind}                        </Badge>                      </div>                    </li>                  ))}                </ul>              </section>            ))          )}        </div> diff --git a/frontend/src/components/workbench/PipelineRail.tsx b/frontend/src/components/workbench/PipelineRail.tsx index 0ddf746..3f0a89e 100644 --- a/frontend/src/components/workbench/PipelineRail.tsx +++ b/frontend/src/components/workbench/PipelineRail.tsx @@ -127,21 +127,21 @@ export function PipelineRail({          {workflow ? (            <div className="mt-2">              <Badge                tone={                  workflow.status === 'blocked'                    ? 'danger'                    : workflow.status.startsWith('waiting')                      ? 'warning'                      : workflow.status === 'completed'                        ? 'success' -                      : 'brand' +                      : 'action'                }              >                {WORKFLOW_STATUS_LABELS[workflow.status]}              </Badge>            </div>          ) : null}        </div>        <ol className="flex-1 space-y-1 overflow-auto p-2">          {stages.map((stage, index) => {            const status = resolveStageStatus(stage, workflow) diff --git a/frontend/src/pages/ProjectListPage.tsx b/frontend/src/pages/ProjectListPage.tsx index 8275f09..d750a3e 100644 --- a/frontend/src/pages/ProjectListPage.tsx +++ b/frontend/src/pages/ProjectListPage.tsx @@ -158,21 +158,21 @@ export function ProjectListPage() {                    className="flex items-center justify-between gap-4 px-5 py-4 hover:bg-slate-50"                  >                    <div className="min-w-0">                      <Link                        to={`/projects/${project.id}/workbench`}                        className="text-base font-medium text-ink hover:text-brand-600"                      >                        {project.title || '鏈懡鍚嶉」鐩?}                      </Link>                      <div className="mt-1 flex flex-wrap items-center gap-2 text-xs text-ink-muted"> -                      <Badge tone={project.entry_type === 'original_track' ? 'brand' : 'gold'}> +                      <Badge tone={project.entry_type === 'original_track' ? 'action' : 'gold'}>                          {entryLabel(project.entry_type)}                        </Badge>                        {project.status ? (                          <Badge tone="default">                            {STATUS_LABEL[project.status] ?? project.status}                          </Badge>                        ) : null}                        {project.episode_count ? <span>{project.episode_count} 闆?/span> : null}                        {project.updated_at ? (                          <span>鏇存柊浜?{formatRelativeTime(project.updated_at)}</span>

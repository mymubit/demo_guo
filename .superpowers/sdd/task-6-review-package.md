# Task6
BASE 1ac929a25a7a500487f1b7ac0f19e9f260ce37ac
HEAD 0d434cf6f801eb5aa6c89f412adaf341ade72ab0

 frontend/src/components/layout/PageShell.test.tsx | 16 ++++++++  frontend/src/components/layout/PageShell.tsx      | 47 +++++++++++++++++++++++  2 files changed, 63 insertions(+)
diff --git a/frontend/src/components/layout/PageShell.test.tsx b/frontend/src/components/layout/PageShell.test.tsx new file mode 100644 index 0000000..bc42910 --- /dev/null +++ b/frontend/src/components/layout/PageShell.test.tsx @@ -0,0 +1,16 @@ +import { render, screen } from '@testing-library/react' +import { describe, expect, it } from 'vitest' +import { PageShell } from './PageShell' + +describe('PageShell', () => { +  it('renders cold-mist header border token', () => { +    const { container } = render( +      <PageShell title="椤圭洰鍒楄〃" description="绠＄悊椤圭洰"> +        <div>鍐呭</div> +      </PageShell>, +    ) +    expect(screen.getByRole('heading', { name: '椤圭洰鍒楄〃' })).toBeInTheDocument() +    const header = container.querySelector('header') +    expect(header?.className).toMatch(/border-border/) +  }) +}) diff --git a/frontend/src/components/layout/PageShell.tsx b/frontend/src/components/layout/PageShell.tsx new file mode 100644 index 0000000..5a25c5d --- /dev/null +++ b/frontend/src/components/layout/PageShell.tsx @@ -0,0 +1,47 @@ +import type { ReactNode } from 'react' +import { cn } from '@/utils/cn' + +type PageWidth = 'fluid' | 'form' | 'narrow' + +const widthClass: Record<PageWidth, string> = { +  /** 鍒楄〃 / 杩愮淮 / 鏃ュ織锛氬悆婊″彲鐢ㄥ搴?*/ +  fluid: 'max-w-[min(100%,90rem)]', +  /** 璁剧疆涓庤〃鍗曪細绋嶅浣嗕粛鍙 */ +  form: 'max-w-5xl', +  /** 鏋佺畝琛ㄥ崟锛堢櫥褰曠被闄ゅ锛?*/ +  narrow: 'max-w-3xl', +} + +interface PageShellProps { +  title: string +  description?: ReactNode +  actions?: ReactNode +  children: ReactNode +  width?: PageWidth +  className?: string +} + +/** 缁熶竴椤甸潰楠ㄦ灦锛氶〉澶?+ 鍙厤缃増蹇冿紝閬垮厤鍚勯〉绐勬煴鍚勮嚜涓烘斂銆?*/ +export function PageShell({ +  title, +  description, +  actions, +  children, +  width = 'fluid', +  className, +}: PageShellProps) { +  return ( +    <div className={cn('mx-auto w-full px-6 py-7 sm:px-8', widthClass[width], className)}> +      <header className="mb-6 flex flex-wrap items-start justify-between gap-4 border-b border-border pb-5"> +        <div className={cn('min-w-0', width === 'fluid' ? 'max-w-4xl' : 'max-w-3xl')}> +          <h1 className="text-xl font-semibold tracking-tight text-ink">{title}</h1> +          {description ? ( +            <div className="mt-1.5 text-sm leading-relaxed text-ink-muted">{description}</div> +          ) : null} +        </div> +        {actions ? <div className="flex shrink-0 flex-wrap items-center gap-2">{actions}</div> : null} +      </header> +      {children} +    </div> +  ) +}
